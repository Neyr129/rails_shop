<%= form_for(@item) do |f| %>
  <% if @item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:</h2>
      <ul>
        <% @item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %> 

  <%= f.text_field :name %> 
  <br> 

  <div class="actions">
    <%= link_to 'Add Image', add_image_path(@item) %>
  </div> 
  <br>


  <div class="actions">
    <%= f.submit  "comfirm",  :class => "button"%>
  </div> 
<% end %>